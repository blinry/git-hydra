#include <map>
#include <set>

class Index {
    public:
        Index(NodeFactory& factory) : factory(factory) {
        }
        void update() {
        }
    private:
        NodeFactory factory;
};
